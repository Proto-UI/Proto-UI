import { cap } from '@proto.ui/core';
import type { VisibilityHostBridge } from './types';

export const VISIBILITY_HOST_BRIDGE_CAP = cap<VisibilityHostBridge>(
  '@proto.ui/visibility/host-bridge'
);
