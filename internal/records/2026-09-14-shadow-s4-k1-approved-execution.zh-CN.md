# S4 目标模式与 K1 批准执行

日期：2026-09-14。状态：user-approved K1 / delegated bounded execution，non-normative。

用户明确采用 K1，并授权 Agent 自行决定 S4 内与 K1 同等规模的问题，持续实施、验证和分节点本地提交，用户仅在 S4 人工验收时介入。工作模式仍为 human-assisted/current-user；目标模式不改变 contributor 身份或外部权限。

批准范围沿用 `2026-09-14-shadow-s4-approved-goal.zh-CN.md` 与 `2026-09-14-shadow-s4-admission-and-portal-decision.zh-CN.md`。旧待决策文字保留为历史；本记录确立后续授权。若必须扩大既定排除范围，仍需回到用户；不推断 push/merge/release 等外部写入。

## K1 执行解释

- 两个精确负半尺寸位移归属 canonical `root-geometry`，不是 `placement`。此前提案的 Root geometry / placement 简写以现有独立 domain vocabulary 为准，规范由 `D-FEEDBACK-STYLE-ROLE-RESOLUTION-0001` S–T 承接。
- 有界 fade/zoom 与居中共同保证整个 Root 的几何及命中，surface 保留绘制；作者无需新增 API，artifact v1 ABI 与其它 profiles 不变。新 recipe 须有独立能力证据，旧 companion 保持明确拒绝。
- body portal 按真实物理树继承主题，验收使用 document 主题并覆盖打开期间切换；不自动复制来源祖先 CSS 变量，不改变 Maker slot 所有权。
- 首次激活失败后的滚动锁等残余资源单独建立证据，修复不自动接管父级受控 open，也不借此实现通用跨实例事务。

## 实施节点

1. 批准记录与 draft criteria / planned evidence。
2. 精确分类、CLI/WC recipe、完整公开 Dialog 准入和动画浏览器证据。
3. Dialog 的焦点、AX、dismissal、portal 与 view/owner 清理问题及组合回归。
4. 完整 S3 Tabs 设置区、Light/split/mixed demo-matrix 固定入口、自动路径与人工清单。
5. S1–S3 回归与最终交付记录，达到 S4 可人工验收后完成目标，不代替用户作人工验收声明。

每个完成节点本地提交，只选择本任务路径，保留既有无关 staged/unstaged/untracked 变更。需要调整路线时记录依据，不将 draft 提升为 active，也不把自动测试成功等同于全浏览器支持。
