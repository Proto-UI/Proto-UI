---
title: 'UI Libraries'
description: 'Choose a component or icon library by visual language, use case, and provenance.'
---

import UiLibraryGallery from '@/components/UiLibraryGallery.astro';

Proto UI is not one component library. It is a foundation for choosing the visual language that fits your product while keeping reusable interaction protocols underneath.

Compare the current libraries below. The previews show each library's visual direction; the credits distinguish the original design or icon source from the Proto UI prototype implementation.

<UiLibraryGallery locale="en" />
