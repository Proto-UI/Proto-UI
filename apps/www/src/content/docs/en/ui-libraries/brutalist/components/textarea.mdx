---
title: 'Brutalist Textarea'
description: 'Square lavender Textarea projection over Base Textarea'
---

import { PrototypePreviewer } from '@/components/PrototypePreviewer';
import BrutalistPageStyle from '@/components/BrutalistPageStyle.astro';

<BrutalistPageStyle />

> Status: proposed `0.2.0-rc.7` release candidate. This page describes the draft rc.7 surface before publication.

`brutalist-textarea-root` is a direct visual composition over Base Textarea. It keeps the same single physical native `<textarea>` and adds no new interaction, state, event, accessibility, or ownership protocol.

<div style="background:var(--pui-background);color:var(--pui-foreground);border:2px solid var(--pui-foreground);box-shadow:3px 3px 0 var(--pui-foreground);padding:1rem;">
  <PrototypePreviewer
    demoId="demo-brutalist-textarea"
    initialRuntime="wc"
    runtimes={['wc', 'react', 'vue']}
  />
</div>

## Inherited Behavior

All behavior comes from `base-textarea-root`: stable controlled/uncontrolled value ownership, normalized `valueChange` and `change` signals, normalized IME composition events, composition-safe controlled restoration, native textarea prop projection, physical focus/blur methods, and textbox name/description accessibility projection.

The Brutalist prototype therefore retains Base's contentless ownership boundary. It does not add children, form submission ownership, rich-text editing, status/live-region semantics, or any second physical control.

## Visual Delta

The entire delta is visual:

- native resizing remains host- or consumer-owned, with no automatic height measurement
- square geometry
- a flat lavender surface
- ink text and border
- a hard offset ink shadow

These styles are applied directly to the Base-backed native textarea. They do not create another protocol layer or change the normalized event payloads.

## References

- Prototype: [P-BRUTALIST-TEXTAREA](https://github.com/Proto-UI/Proto-UI/blob/main/spec/prototypes/P-BRUTALIST-TEXTAREA.yaml)
- Base protocol: [P-BASE-TEXTAREA](https://github.com/Proto-UI/Proto-UI/blob/main/spec/prototypes/P-BASE-TEXTAREA.yaml)
- Source: [packages/prototypes/brutalist/src/textarea](https://github.com/Proto-UI/Proto-UI/tree/main/packages/prototypes/brutalist/src/textarea)
