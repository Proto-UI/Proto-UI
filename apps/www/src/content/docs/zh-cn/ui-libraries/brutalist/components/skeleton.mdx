---
title: Skeleton
sidebar:
  order: 8
---

import { PrototypePreviewer } from '@/components/PrototypePreviewer';
import BrutalistPageStyle from '@/components/BrutalistPageStyle.astro';

<BrutalistPageStyle />
> 可用状态：`@proto.ui/prototypes-brutalist@0.2.0` 已通过 npm `latest` 随 [v0.2.0](https://github.com/Proto-UI/Proto-UI/releases/tag/v0.2.0) 发布。生命周期：[P-BRUTALIST-SKELETON](https://github.com/Proto-UI/Proto-UI/blob/main/spec/prototypes/P-BRUTALIST-SKELETON.yaml) 仍为 `draft`；本页描述已发布并经过测试的 0.2 surface，不构成 active 协议保证。

Brutalist Skeleton 是一个直接定义的 styled-only 视觉占位原型：静态方形 Yandu 薰衣草紫表面，带有 ink 边框和硬阴影。它是 passive、contentless、从 accessibility tree 隐藏的，也不拥有重复 shimmer 或 pulse。

当未解析布局需要视觉形状反馈时，可以把它放在父级 async/loading region 中。loading 与 `aria-busy` 状态、announcement、占位内容替换时机和焦点连续性均由父级拥有；Skeleton 不创建这些语义。若应用额外添加 motion，也必须尊重 reduced-motion 偏好。

<div class="brutalist-demo-frame">
  <PrototypePreviewer demoId="demo-brutalist-skeleton" initialRuntime="wc" />
</div>
