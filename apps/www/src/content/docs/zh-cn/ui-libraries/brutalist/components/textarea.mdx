---
title: 'Brutalist Textarea'
description: '基于 Base Textarea 的方角薰衣草紫投影'
---

import { PrototypePreviewer } from '@/components/PrototypePreviewer';
import BrutalistPageStyle from '@/components/BrutalistPageStyle.astro';

<BrutalistPageStyle />

> 可用状态：`@proto.ui/prototypes-brutalist@0.2.0` 已通过 npm `latest` 随 [v0.2.0](https://github.com/Proto-UI/Proto-UI/releases/tag/v0.2.0) 发布。生命周期：[P-BRUTALIST-TEXTAREA](https://github.com/Proto-UI/Proto-UI/blob/main/spec/prototypes/P-BRUTALIST-TEXTAREA.yaml) 及其继承的 Base 协议仍为 `draft`；本页描述已发布并经过测试的 0.2 surface，不构成 active 协议保证。

`brutalist-textarea-root` 是 Base Textarea 上的直接视觉组合。它保留同一个物理原生 `<textarea>`，不新增 interaction、state、event、accessibility 或 ownership 协议。

<div class="brutalist-demo-frame">
  <PrototypePreviewer
    demoId="demo-brutalist-textarea"
    initialRuntime="wc"
    runtimes={['wc', 'react', 'vue']}
  />
</div>

## 继承的行为

全部行为都来自 `base-textarea-root`：明确的受控/非受控 value 所有权、归一化的 `valueChange` 与 `change` signal、归一化的 IME composition event、composition-safe 受控值恢复、原生 textarea props 投影、物理 focus/blur method，以及 textbox name/description 无障碍投影。

因此 Brutalist 原型保留 Base 的 contentless 所有权边界。它不增加 child、form submission 所有权、rich-text editing、status/live-region 语义，也不创建第二个物理控件。

## 视觉差量

全部差量都属于视觉层：

- 原生尺寸调整仍由 host 或 consumer 拥有，不进行自动高度测量
- 方角几何
- 平涂薰衣草紫表面
- ink 文本与边框
- 硬偏移 ink 阴影

这些样式直接应用到由 Base 支撑的原生 textarea，不会建立新的协议层，也不会改变归一化事件载荷。

## 参考

- 原型：[P-BRUTALIST-TEXTAREA](https://github.com/Proto-UI/Proto-UI/blob/main/spec/prototypes/P-BRUTALIST-TEXTAREA.yaml)
- Base 协议：[P-BASE-TEXTAREA](https://github.com/Proto-UI/Proto-UI/blob/main/spec/prototypes/P-BASE-TEXTAREA.yaml)
- 源码：[packages/prototypes/brutalist/src/textarea](https://github.com/Proto-UI/Proto-UI/tree/main/packages/prototypes/brutalist/src/textarea)
